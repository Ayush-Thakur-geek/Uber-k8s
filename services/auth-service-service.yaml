apiVersion: v1
kind: Service
metadata:
  name: auth-service-service
spec:
  type: NodePort
  selector:
    app: auth-service
    tier: backend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30080
