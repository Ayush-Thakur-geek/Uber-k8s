apiVersion: v1
kind: Service
metadata:
  name: cloud-service-service
  labels:
    app: cloud-service
    tier: backend
spec:
  type: NodePort
  selector:
    app: cloud-service
    tier: backend
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 32767